{
  "name": "file-analysis-tool",
  "version": "1.0.0",
  "description": "File analysis tool with Electron, Express, and React",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "server": "node server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "electron": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "@huggingface/transformers": "^2.4.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
